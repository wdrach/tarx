export enum LogLevel {
  INFO = 'info',
  ERROR = 'error',
  WARN = 'warn',
  SUCCESS = 'success'
}

export enum Granularity {
  MINUTE = 60,
  FIVE_MINUTE = 300,
  FIFTEEN_MINUTE = 900,
  HOUR = 3600,
  SIX_HOUR = 21600,
  DAY = 86400
}